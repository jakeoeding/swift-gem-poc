name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    name: CI (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Import Swift signing keys (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          curl -fsSL --compressed --retry 3 https://www.swift.org/keys/all-keys.asc | gpg --import -

      - name: Setup Swift
        uses: jakeoeding/setup-swift@test-swiftly-bump
        with:
          swift-version: "6.4.0"

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.1"
          bundler-cache: true

      - name: Set SDKRoot (macOS)
        if: matrix.os == 'macos-latest'
        run: echo "SDKROOT=$(xcrun --show-sdk-path)" >> $GITHUB_ENV

      - name: Build gem
        run: gem build swift_counter.gemspec

      - name: Install gem
        run: gem install swift_counter

      - name: Smoke test
        run: echo "c = SwiftCounter::Counter.new; c.increment; c.count == 1" | bin/console
